export const helpMessage: string = 

// Text that will be displayed when running 'zircon help'

`
zircon <command> <flags>

Usage:

zircon build		compile the current Zircon project
	-t	compile TypeScript before anything
zircon init 		initilise an empty Zircon project
	-c	use the config CLI
	-t	set up a TypeScript ready environment
`
//zircon -h <command> quick help on <command>